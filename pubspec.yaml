name: mongodb_by_rest_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
# repository: https://github.com/Suebersson/mongodb_by_rest_api

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  crypto: ^3.0.7
  http: ^1.6.0
  convert_json:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.2